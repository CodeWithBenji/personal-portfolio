pipeline:
  build:
    image: node:18-alpine
    environment:
      - NODE_ENV=production
    commands:
      - ls
      - npm install
      - ls node_modules
      - npm run build

branches:
  include: [ main, feature/* ]